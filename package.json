{
  "private": true,
  "scripts": {
    "build": "esbuild js/htmx.js js/player.js js/editor.js --bundle --platform=browser --format=iife --minify --outdir=static"
  },
  "dependencies": {
    "htmx.org": "^2.0.4",
    "lodash-es": "^4.17.21",
    "sortablejs": "^1.15.6"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
